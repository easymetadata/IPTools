# Core dependencies for IP enrichment tool
pandas>=1.5.0
tabulate>=0.9.0
requests>=2.28.0
PyYAML>=6.0.0
geoip2>=4.5.0
netaddr>=0.8.0
joblib>=1.2.0

# Flask dependencies (for web interface)
Flask>=2.2.0
gunicorn>=20.1.0

# Optional: Excel support
openpyxl>=3.0.0
